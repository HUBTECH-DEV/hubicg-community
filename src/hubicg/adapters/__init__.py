"""Read-only exporters that render HubICG roles into a tool's native format.

See docs/adr/ADR-002-tool-adapter-boundary.md: adapters consume the public
role schema and never add tool-specific fields to the core role modules.
"""
